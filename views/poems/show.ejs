<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <link rel="stylesheet" href="/style.css">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Document</title>
    </head>
    <body>
        <div>
            <% poems.forEach((poem, idx) => { %>
                <li>
                    <label>Poem #<%= idx + 1 %></label>
                    <p><%= poem.poem %></p>
                </li>
            <% }); %>
        </div>
    </body>
</html>